package routes

import "github.com/gofiber/fiber/v2"

// SetupHistoryRoutes registers scan history routes
func SetupHistoryRoutes(v1 fiber.Router) {
	// Will be implemented in EPIC 9
	// v1.Get("/history", controllers.GetHistory)
	_ = v1
}